export interface NavItem {
	name: string;
	path: string;
}
export interface DataCards {
	img: string;
	title: string;
	text: string;
}

export interface TextButton {
	text: string;
	button: string;
}
